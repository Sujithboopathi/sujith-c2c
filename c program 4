#include <stdio.h>

void addThreeNumbers(int num1, int num2, int num3) 
{
    int sum = num1 + num2 + num3;  
    printf("The sum of %d, %d, and %d is: %d\n", num1, num2, num3, sum);
}

int main() {
    
    addThreeNumbers(10, 20, 30);

    return 0; 
}
